import 'package:flutter/material.dart';

class Corrigir extends StatelessWidget {
  const Corrigir({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Correção de provas')),
      body: const Center(child: Text('Correção de provas')),
    );
  }
}
